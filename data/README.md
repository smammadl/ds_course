This directory contains the data for the project and optionally the scripts to create a docker container with the data.

To create a docker container with the data:
```bash
docker compose -f path/to/docker-compose.yml up -d
```